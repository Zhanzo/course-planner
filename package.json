{
  "name": "course_planner",
  "version": "0.0.0",
  "main": "main.js",
  "repository": "",
  "scripts": {
    "heroku-prebuild": "cd client/ && npm install && npm install -g @angular/cli",
    "heroku-postbuild": "cd client/ && npm run build"
  },
  "engines": {
    "node": "16.0.0",
    "npm": "7.11.2"
  },
  "cacheDirectories": [
    "client/node_modules"
  ],
  "dependencies": {
    "@angular/animations": "^12.0.2",
    "@angular/cdk": "^12.0.2",
    "@angular/material": "^12.0.2"
  }
}
